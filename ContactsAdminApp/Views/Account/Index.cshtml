
@{
    ViewBag.Title = "Index";
}

<h2>List of Roles</h2>

@Html.ActionLink("Create New Role", "Create") | @Html.ActionLink("Add Users to Role", "ManageUserRoles")
<hr />
<div>
    @foreach (var role in Model)
    {
        <p>
            <strong>@role.Name</strong>
        </p>
    }
</div>